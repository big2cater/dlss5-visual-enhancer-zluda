/build
/dist
/build_qt*
/run
*.zip
*.log
*.err
.release-staging*
speed-test-staging*
DLSSNRFilter-*/
/build_gpu_check/
/bin
# Written next to the script by fix_tdr.ps1; belongs to the machine, not the repo.
# Timestamped since 2026-09-15: the fixed name meant each run overwrote the last.
tdr_backup_before_fix*.txt
